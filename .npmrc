# O npm recente bloqueia install scripts por padrão (allow-scripts). O sharp
# depende do seu script de install para preparar o binário nativo da plataforma;
# sem isso a função serverless quebra com "Could not load the sharp module".
allow-scripts=true
